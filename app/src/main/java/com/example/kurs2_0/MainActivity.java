package com.example.kurs2_0;import android.app.AlarmManager;import android.app.PendingIntent;import android.content.Intent;import android.os.Bundle;import com.google.android.material.floatingactionbutton.FloatingActionButton;import com.google.android.material.snackbar.Snackbar;import androidx.appcompat.app.AppCompatActivity;import androidx.appcompat.widget.Toolbar;import androidx.fragment.app.Fragment;import androidx.fragment.app.FragmentManager;import androidx.fragment.app.FragmentTransaction;import androidx.transition.FragmentTransitionSupport;import android.view.View;import android.view.Menu;import android.view.MenuItem;import android.widget.Button;import android.widget.CheckBox;import android.widget.CompoundButton;import android.widget.EditText;import android.widget.FrameLayout;import android.widget.LinearLayout;import android.widget.TextView;import java.util.Calendar;public class MainActivity extends AppCompatActivity implements View.OnClickListener,OnFragmentAddDataListener  {    Habit name;    TextView tx;    LinearLayout container ;    int wrapContent = LinearLayout.LayoutParams.WRAP_CONTENT;    private EditText editText;    private TextView textView;    private Button button;    private FrameLayout fragmentContainer;    private FrameLayout fragmentContainer2;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        Toolbar toolbar = findViewById(R.id.toolbar);        setSupportActionBar(toolbar);        FloatingActionButton fab = findViewById(R.id.fab);        fab.setOnClickListener(this) ;        Intent intent = new Intent(MainActivity.this, ReminderBroadcast.class);        PendingIntent pendingIntent = PendingIntent.getBroadcast(MainActivity.this, 0, intent, 0);        AlarmManager alarmManager = (AlarmManager) getSystemService(ALARM_SERVICE);        Calendar currentTime = Calendar.getInstance();        currentTime.get(Calendar.HOUR_OF_DAY);        currentTime.get(Calendar.MINUTE);        currentTime.get(Calendar.SECOND);        Calendar notificationTime = Calendar.getInstance();        notificationTime.set(Calendar.HOUR_OF_DAY, 13);        notificationTime.set(Calendar.MINUTE, 35);        notificationTime.set(Calendar.SECOND, 0);        long delta;        long currentSystemTime = System.currentTimeMillis();        long newTime;            delta = notificationTime.getTimeInMillis()- currentTime.getTimeInMillis();            if (delta > 0 && (Calendar.DAY_OF_WEEK==7)) {                newTime = currentSystemTime + delta;                alarmManager.set(AlarmManager.RTC_WAKEUP, newTime, pendingIntent);            } else if (delta < 0&&(Calendar.DAY_OF_WEEK==7)) {                newTime = currentSystemTime + delta + 24 * 6 * 6 * 1000;                alarmManager.set(AlarmManager.RTC_WAKEUP, newTime, pendingIntent);            }      /*  FragmentManager fma = getSupportFragmentManager();        Fragment fragment = fma.findFragmentById(R.id.add);        if (fragment == null) {            fragment = new HomeFragment();            fma.beginTransaction()                    .add(R.id.add, fragment, HomeFragment.TAG)                    .commit();        }*/       // tx = findViewById(R.id.textt);      /*  FragmentManager fm = getSupportFragmentManager();        Fragment fragment = fm.findFragmentById(R.id.blank_fragment);        if (fragment == null) {            fragment = new BlankFragment();        }        fm.beginTransaction()                .add(R.id.blank_fragment, fragment)                .setTransition(FragmentTransaction.TRANSIT_FRAGMENT_OPEN)                .commit();*/    }    @Override    public void onClick(View view) {        switch (view.getId()) {            case R.id.fab:                String data = "";                toFragment();                //fromFragmentData(data);                break;        }    }    public void toFragment() {        FragmentManager fm = getSupportFragmentManager();        Fragment fragment = new BlankFragment();            fm.beginTransaction()                    .add(R.id.add, fragment)                    .commit();    }    public void fromFragmentData(String data) {        //Данные получены            String[] subStr;            subStr = data.split("%");        CheckBox checkBox = new CheckBox(this);        checkBox.setText(subStr[0]+": "+subStr[1]);        container = (LinearLayout)findViewById(R.id.ainer);        LinearLayout.LayoutParams lParams = new LinearLayout.LayoutParams(                wrapContent, wrapContent);        container.addView(checkBox, lParams);        Calendar currentTime = Calendar.getInstance();     //   currentTime.set(Calendar.DAY_OF_WEEK, Calendar.MONDAY);        currentTime.set(Calendar.HOUR,20);        currentTime.set(Calendar.MINUTE,47);        currentTime.set(Calendar.SECOND,0);        setNotification(currentTime.getTimeInMillis());    }public void setNotification (long time){    LinearLayout.LayoutParams lParams = new LinearLayout.LayoutParams(            wrapContent, wrapContent);    Intent intent = new Intent(MainActivity.this, ReminderBroadcast.class);    PendingIntent pendingIntent = PendingIntent.getBroadcast(MainActivity.this , 0, intent, 0);    AlarmManager alarmManager = (AlarmManager)getSystemService(ALARM_SERVICE);    Calendar currentTime = Calendar.getInstance();    currentTime.get(Calendar.HOUR_OF_DAY);    currentTime.get(Calendar.MINUTE);    currentTime.get(Calendar.SECOND);    long delta;    long currentSystemTime = System.currentTimeMillis();    long newTime;        delta = time- currentTime.getTimeInMillis();        if (delta > 0) {            newTime = currentSystemTime + delta;            alarmManager.set(AlarmManager.RTC_WAKEUP, newTime, pendingIntent);        } else if (delta < 0) {            newTime = currentSystemTime + delta + 24 * 6 * 6 * 1000;            alarmManager.set(AlarmManager.RTC_WAKEUP, newTime, pendingIntent);        }}    @Override    public boolean onCreateOptionsMenu(Menu menu) {        // Inflate the menu; this adds items to the action bar if it is present.        getMenuInflater().inflate(R.menu.menu_main, menu);        return true;    }    @Override    public boolean onOptionsItemSelected(MenuItem item) {        // Handle action bar item clicks here. The action bar will        // automatically handle clicks on the Home/Up button, so long        // as you specify a parent activity in AndroidManifest.xml.        int id = item.getItemId();        //noinspection SimplifiableIfStatement        if (id == R.id.action_settings) {            return true;        }        return super.onOptionsItemSelected(item);    }    @Override    public void onOpenFragmentHome(String string) {        FragmentManager fm = getSupportFragmentManager();        Fragment fragment = fm.findFragmentById(R.id.add);        if (fragment instanceof BlankFragment) {            Fragment fragmentReplace;            fragmentReplace = new HomeFragment();            Bundle bundle = new Bundle();            bundle.putString(HomeFragment.KEY, string);            fragmentReplace.setArguments(bundle);            fm.beginTransaction()                    .replace(R.id.container, fragmentReplace, HomeFragment.TAG)                    .setTransition(FragmentTransaction.TRANSIT_FRAGMENT_OPEN)                    .addToBackStack(HomeFragment.TAG)                    .commit();        }    }}